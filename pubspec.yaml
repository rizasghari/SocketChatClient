name: socket_chat_client
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  provider: ^6.0.3
  logger: ^2.3.0
  web_socket_channel: ^3.0.0
  shared_preferences: ^2.2.3
  image_picker: ^1.1.2
  scrollview_observer: ^1.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
